-- --------------------------------------------------------------------
-- Table: FIN_STG_MOVEMENT
-- Purpose: staging table used by the batch loader
-- --------------------------------------------------------------------
DROP TABLE IF EXISTS FIN_STG_MOVEMENT;

CREATE TABLE FIN_STG_MOVEMENT (
    STG_ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TRANSACTION_ID VARCHAR(20) NOT NULL,
    TRANSACTION_DATE DATE NOT NULL,
    ACCOUNT_NUMBER VARCHAR(20) NOT NULL,
    TRANSACTION_TYPE CHAR(2) NOT NULL,
    AMOUNT DECIMAL(13,2) NOT NULL,
    CURRENCY CHAR(3) NOT NULL,
    REFERENCE VARCHAR(35) NOT NULL,
    CHANNEL VARCHAR(10) NOT NULL,
    LOAD_TIMESTAMP TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    PROCESS_STATUS CHAR(1) DEFAULT 'N' NOT NULL
);

-- --------------------------------------------------------------------
-- Index for transaction uniqueness and fast lookup
-- --------------------------------------------------------------------
CREATE UNIQUE INDEX IF NOT EXISTS IX_FIN_STG_TRANS
    ON FIN_STG_MOVEMENT (TRANSACTION_ID);